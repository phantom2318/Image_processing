[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ipress"
version = "0.1.0"
description = "Helper utilities for OpenCV image selection and visualization made by Jupiter"
dependencies = [
    "opencv-python",
    "numpy",
    "matplotlib"
]